import * as paintTypes from "./types";

const initialState: paintTypes.IAppState = {
  dispatch: () => {},
  currentColor: "black",
  paintSocket: null,
};

const holaMundo = () => {

}

export default initialState;
